<h1>Jobs</h1>
<% @jobs.each do |job| %>
  <div class="job">
    <h2><%= link_to job.title, job.url, target: :_blank%></h2>
    <p><%= job.company %></p>
  </div>
<% end %>
<p><%= link_to "Add a job", new_job_path %></p>

<p><%= paginate @jobs %></p>
